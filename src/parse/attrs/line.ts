import { XmlNode } from '@/xmlNode';
import SlideBase from '../slide/slideBase';
import { Line } from './types';

export default async function parseLine(pathNode: XmlNode, slide: SlideBase): Promise<Line> {}
