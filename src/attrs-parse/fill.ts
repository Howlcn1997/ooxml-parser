import { XmlNode } from '../xmlNode';

enum FillType {
  Solid = 'solid',
  Gradient = 'gradient',
  Pattern = 'pattern',
  Picture = 'picture',
}

export function parseFill(fillNode: XmlNode) {}
