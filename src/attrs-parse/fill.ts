import { DocObjectNode } from '../types';

enum FillType {
  Solid = 'solid',
  Gradient = 'gradient',
  Pattern = 'pattern',
  Picture = 'picture',
}

export function parseFill(fillNode: DocObjectNode) {}
